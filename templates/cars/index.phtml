<!DOCTYPE html>
<html>
<head>
    <title>Cars</title>
</head>
<body>
    <h1>Cars</h1>
    
    <?php if (isset($flash['success'])): ?>
        <div style="color: green;"><?= $flash['success'][0] ?></div>
    <?php endif; ?>
    
    <a href="/cars/new">Add New Car</a>
    
    <table border="1">
        <tr>
            <th>ID</th>
            <th>Make</th>
            <th>Model</th>
            <th>Actions</th>
        </tr>
        <?php foreach ($cars as $car): ?>
        <tr>
            <td><?= htmlspecialchars($car->getId()) ?></td>
            <td><?= htmlspecialchars($car->getMake()) ?></td>
            <td><?= htmlspecialchars($car->getModel()) ?></td>
            <td>
                <a href="/cars/<?= $car->getId() ?>">View</a>
                <form action="/cars/<?= $car->getId() ?>/delete" method="POST" style="display: inline;">
                    <button type="submit" onclick="return confirm('Are you sure?')">Delete</button>
                </form>
            </td>
        </tr>
        <?php endforeach; ?>
    </table>
</body>
</html>
